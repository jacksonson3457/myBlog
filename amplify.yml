# /amplify.yml（リポジトリ直下）
version: 1
applications:
  - appRoot: my-blog
    frontend:
      phases:
        preBuild:
          commands:
            - npm ci
        build:
          commands:
            - npm test
            - npm run build
      artifacts:
        baseDirectory: out
        files:
          - "**/*"
      cache:
        paths:
          - node_modules/**/*
          - .next/cache/**/*
          - remoteImagesForOptimization/**/*
